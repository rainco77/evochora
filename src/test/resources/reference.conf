# This is the default configuration file for tests.
# It provides default values that can be overridden.

pipeline {
  # An empty pipeline block is required to prevent the ServiceManager
  # from throwing an exception during initialization in tests.
}

default.value = "default"
a = "default"
b = "default"
cli.value = "default"
file.value = "default"